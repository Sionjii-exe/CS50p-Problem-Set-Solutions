print(input().replace(" ", "..."))

# First, the input() function will have me input some text
# Then, attached to the input() function is the replace() function which is set to replace " " with "..."
# Finally, the print() function will print out the input sentence with the " " replaced with "..."
